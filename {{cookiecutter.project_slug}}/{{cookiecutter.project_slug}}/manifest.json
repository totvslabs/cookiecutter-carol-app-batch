{
  "batch": {
    "processes": [
      {
        "algorithmDescription": {
          "en-US": "{{cookiecutter.algorithm_description_en_US}}",
          "pt-BR": "{{cookiecutter.algorithm_description_pt_BR}}"
        },
        "algorithmName": "{{cookiecutter.algorithm_name}}",
        "algorithmTitle": {
          "en-US": "{{cookiecutter.algorithm_description_en_US}}",
          "pt-BR": "{{cookiecutter.algorithm_description_pt_BR}}"
        },
        "instanceProperties": {
          "environments": {
            "EXAMPLE_ENV_1": "value_1",
            "EXAMPLE_ENV_2": "value_2"
          },
          "luigi": {
            "workers": "{{cookiecutter.luigi_workers}}"
          },
          "properties": {
            "dockerImage": "{{cookiecutter.carol_app_docker_image}}",
            "flavor": "{{cookiecutter.carol_app_machine_types}}"
          }
        },
        "name": "{{cookiecutter.project_slug}}"
      }
    ]
  }
}
